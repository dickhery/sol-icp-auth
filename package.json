{
  "dependencies": {
    "@dfinity/agent": "^3.1.0"
  },
  "name": "sol_icp_poc",
  "version": "1.0.0",
  "scripts": {
    "build": "cd src/sol_icp_poc_frontend/assets && npm install && npm run build"
  }
}
